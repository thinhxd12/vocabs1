import type { Load } from "@sveltejs/kit";
import { getSpotlightImage_v4 } from "$lib/functions";

// export const load: Load = async ({ fetch, setHeaders }) => {
//   const data = await getSpotlightImage_v4(fetch, setHeaders);
//   return data;
// };
